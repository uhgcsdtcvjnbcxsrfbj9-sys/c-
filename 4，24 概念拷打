栈内存 和 堆内存 核心区别？各自生命周期？
char *str 与 char str[] 本质区别？
什么是野指针？产生原因 + 怎么规避？
malloc 申请的内存，程序退出会自动释放吗？为什么？
局部变量、全局变量、静态变量 static 分别存在哪个内存区？
答：
1. 栈（Stack）由系统自动分配、自动释放，空间小、固定、读写快。存放：局部变量、函数形参、函数临时数据。生命周期：函数调用时开辟，函数执行结束立刻回收。超出限制会栈溢出崩溃
堆（Heap）需要程序员手动申请、手动释放/。malloc / calloc / realloc / free。空间大、灵活、可动态扩容。存放：程序运行时动态需要的内存。生命周期：手动 malloc 分配后，直到手动 free 或程序结束才释放
不及时 free → 内存泄漏；频繁申请释放 → 内存碎片
2， 内存存储位置不同
char *str
字符串常量存储在 只读数据段 (.rodata)，属于只读内存。
char str[]
字符串内容会拷贝到当前函数栈空间，属于可读可写内存。
是否可修改内容
char *str：不能修改字符串内容
str[0] = 'a'; 会触发段错误、程序崩溃。
char str[]：可以随意修改数组内容
str[0] = 'a'; 正常运行。
变量本质
char *str：是一个指针变量，保存字符串首地址。
char str[]：是一个字符数组，数组名是数组首地址常量。
能否二次赋值
char *str：可以
str = "new string"; 让指针指向新字符串。
char str[]：不可以
数组名是常量，不能直接赋值修改指向。
 大小区别
sizeof(char*str)：永远是 4 字节 / 8 字节（指针大小）
sizeof(char str[])：字符串实际长度 + 结束符 \0

3，题目：什么是野指针？产生原因？
定义
野指针：指针变量指向一块 未知、不受管控、非法、未申请 的内存地址。
没有合法指向、操作系统没给你授权访问，就是野指针。
 三大产生原因
指针未初始化
char *p;   // 局部指针，默认随机垃圾地址，直接就是野指针
指针指向的内存已经释放 / 销毁
局部变量函数结束销毁、free 释放内存后，指针没置空，变成野指针（悬空指针）。
指针越界访问
数组访问超范围、指针乱偏移，跑到不属于自己的内存区域。
危害
程序段错误、崩溃、莫名死机、内存踩踏、变量莫名被改，嵌入式设备死机大多和野指针有关。
怎么规避
指针定义立刻初始化：char *p = NULL;
free 用完立刻手动置空：p = NULL;
严格限制指针偏移、数组下标范围

malloc 申请的内存，程序正常退出会自动释放吗？
程序运行期间：malloc 堆内存必须手动 free，不释放 = 内存泄漏。
程序正常终止退出后：
操作系统会回收该进程所有资源，所有堆内存自动全部释放。



局部变量、全局变量、static 静态变量分别存在哪个内存区域？
局部变量，存：栈区 stack，生命周期：函数运行时创建，函数结束立刻销毁。
全局变量 在 全局数据段
static！也在 全局数据段
5大内存区域 通俗解释（最好记）
1. 栈区（stack）
存放：
局部变量、函数形参、临时数据
特点：
自动分配、自动释放，空间小、速度快
2. 堆区（heap）
存放：
malloc / calloc / realloc 手动申请的内存
特点：
手动申请、手动 free，空间大，不用就泄露
3. 全局数据段（.data/.bss）【你问的「全局段」】
存放：
全局变量
static 静态变量（局部静态 + 全局静态都在这）
特点：
程序一运行就存在，整个程序生命周期不销毁
函数结束也不会消失，和栈完全不一样
4. 只读常量区（.rodata）
存放：
字符串常量 比如 char *p = "hello" 里的 hello
 代码段（.text）
存放：
你写的所有函数代码、程序指令



指针数组 和 数组指针
指针数，本质：一个数组，里面存的全是指针
定义：char *arr[5];
理解：数组的每个元素，都是一个指针地址。，用途：常用来存多个字符串、多个地址。先数组，后指针 → 指针数组，存一堆指针
 数组指针，本质：一个指针，专门用来指向一整个数组。定义：char (*p)[5];
 指针数组：是数组，每个元素为指针，多用于存放多字符串。
数组指针：是指针，专门指向一个固定长度的数组，常用于二维数组操作。


void* 是什么指针？为什么不能直接解void * 叫无类型指针 / 通用指针
可以接收任意类型指针的地址，不限定数据类型。只存地址，没有绑定类型大小，
不能直接解引用、不能做加减偏移。
原因：编译器不知道它指向的数据占多少字节，无法确定访问内存的长度。引用？
void* p
只存地址，没有绑定类型大小，编译器不知道：
是 int (4 字节)、char (1 字节) 还是 short (2 字节)。
所以：
不能 *p 解引用（不知道取几个字节）
不能 p++ 指针偏移（不知道一步跳几字节）
必须强制类型转换 (int *)p
告诉编译器：按 int 类型解析，才能读写、加减。




指针 p + 1 加的是 1 个字节吗？为什么？
指针的加减，不是按字节加，
而是以自身指向的数据类型大小为单位偏移。
int* p → p+1 偏移 4 字节
char* p → p+1 偏移 1 字节
short* p→ p+1 偏移 2 字节



. 和 -> 访问结构体成员，本质区别是什么？
使用对象不同
. ：结构体普通变量 访问成员
-> ：结构体指针 访问成员
. ：直接操作结构体实体内存
-> ：语法简写，本质等价于：(*结构体指针).成员
struct Node n;      // 结构体变量
n.data = 1;         // 用 .
struct Node *p = &n;// 结构体指针
p->data = 2;        // 用 ->



什么是 结构体内存对齐？为什么 C 语言、嵌入式必须对齐？
CPU 不会按逐个字节随便读取内存，
编译器会自动给结构体成员补空白字节（填充），
让每个成员的起始地址，都落在固定对齐边界上，
对齐边界就是规定好的固定间隔数值；
这套规则，就叫结构体内存对齐。
硬件 CPU 要求
很多架构（ARM / 单片机）不支持非对齐地址访问，
不对齐会直接死机、报错、硬件异常。
提高读写效率
CPU 一次读 4/8 字节，对齐后一次读完；
非对齐要拆成多次读取，速度变慢。
避免数据错乱
跨平台、网络通信、硬件寄存器映射时，
不对齐会导致结构体大小错位、数据解析错误。



const 修饰指针，三种写法分别代表什么含义？
// 写法1
const int *p;
// 写法2
int *const p;
// 写法3
const int *const p;
const 修饰谁，谁就不能改
const 在 * 左边：内容不能改
const 在 * 右边：地址不能改
1. const int *p;
✅ 指针地址可以改
❌ 指向的数据不能改
可读不可改，指针能乱跑
2. int *const p;
✅ 指向的数据可以改
❌ 指针地址固定不能改
指针钉死不动，内容随便改
3. const int *const p;
❌ 地址不能改
❌ 内容也不能改
双重锁定，全都不能动


简述：数组名 和 & 数组名 的区别？
数组名 arr
代表：数组首元素的地址
步长：+1 跳过 1 个元素
&arr
代表：整个数组的地址
步长：+1 跳过一整个数组



题目：指针和数组的区别？作答
数组：是连续开辟的一块内存空间，存放同类型数据，是内存实体。
指针：是一个变量，只存内存地址，本身不存数据。
大小不同
sizeof(数组)：算出整个数组总字节大小。
sizeof(指针)：永远是固定值（4/8 字节），和指向内容无关。
地址特性
数组名是常量地址，不能被赋值、不能自增自减。
指针是变量，可以随意修改指向、++/--、赋值。


strlen 和 sizeof 有什么区别？
strlen()
库函数，只针对字符串
从首地址开始计数，遇到 \0 停止
不包含结束符 \0
运行时计算
sizeof
C 运算符，不是函数
计算变量 / 类型 / 数组占用的总内存字节数
包含 \0、补齐填充字节
编译时计算



题目：局部变量、全局变量、静态局部变量 三者区别？作答。
局部变量
定义在函数内部，作用域仅限当前函数；栈区存储；函数调用结束立即销毁；默认无初始值，随机垃圾值。
全局变量
定义在函数外部，整个工程都可使用；全局静态区存储；程序全程存在；默认自动初始化为 0。
静态局部变量（static 修饰局部）
作用域仍在当前函数内部；全局静态区存储；程序全程存活，不会随函数结束销毁；只初始化一次，默认初始化为 0，保留上一次的值。


题目：static 关键字一共有哪三个作用？
修饰局部变量
延长生命周期，从「栈区」搬到「静态区」，程序运行全程存在，只初始化一次，保留上次运算结果，作用域不变（仍仅限本函数）。
修饰全局变量
限制作用域，仅当前源文件内有效，不能被其他文件 extern 引用，实现文件私有化。
修饰函数
函数只能在当前源文件内部调用，外部文件无法访问，隔离代码、防止命名冲突。


题目：堆和栈的区别？（C 语言 / 嵌入式必考）
管理方式
栈：系统自动申请、自动释放；
堆：手动 malloc/free 申请释放，不手动释放会内存泄漏。
内存空间
栈：空间小，地址连续、向下增长；
堆：空间大，地址碎片化、向上增长。
生命周期
栈：局部变量随函数调用创建，函数结束立刻销毁；
堆：生命周期由程序员控制，直到手动释放或程序结束。
安全性
栈：固定大小，容易栈溢出；
堆：灵活，但容易野指针、内存泄漏。



什么是野指针？野指针产生的原因 + 危害？
野指针定义
指针指向非法、未申请、无权限的内存地址，没有明确合法指向的指针。
产生原因
指针未初始化，随机指向垃圾地址；
指针指向的内存被释放（free），但指针未置空；
指针越界访问数组 / 内存。
危害
程序崩溃、段错误；
莫名篡改内存数据，产生诡异 bug；
嵌入式中可能引发硬件异常。



malloc、calloc、realloc 三者区别？来作答。
1. malloc
动态开辟堆内存
不会自动初始化，内存是随机脏数据
用法：malloc(字节数)
2. calloc
动态开辟堆内存
自动全部初始化为 0
用法：calloc(元素个数, 单个元素字节大小)
3. realloc
扩容 / 缩宽已有的堆内存
可在原地址扩容，也会重新开辟新空间、拷贝旧数据、释放旧空间
用来修改已经 malloc/calloc 申请的内存大小
malloc 单纯开空间，随机值；
calloc 开好直接清零；
realloc 专门改大小。


题目：#define 宏定义 和 const 常量有什么区别？
#define PI 3.14159
作用：纯文本替换


const double PI = 3.14159;
作用：定义一个只读变量
有明确数据类型（int、double…）
本质是变量，只是不能修改

程序编译前，全文所有 PI 直接换成 3.14159
#define：预处理阶段简单文本替换，无类型检查
const：是带类型的常量，编译阶段有效，有严格类型检查
2. 类型与安全
宏：无类型，容易产生隐性运算错误、边界问题
const：有明确数据类型，语法严谨，报错提示清晰
3. 作用域
宏：默认全局有效，可跨文件，undef 才能取消
const：遵循变量作用域（局部 / 全局），可控
4. 内存占用
宏：不占用内存，纯替换
const：会分配内存空间
5. 可否调试
宏：无法调试
const：可断点调试、查看值



题目：结构体和联合体（共用体）的区别？
结构体 struct
用来把不同类型的变量打包在一起，当做一个整体。
比如：学生（姓名、年龄、分数），每个成员单独占内存，互不干扰。
联合体 union（共用体）
所有成员共用同一块内存，同一时间只能用其中一个成员。
省内存，适合硬件、寄存器、数据转换场景。
结构体：总大小 = 所有成员字节之和 + 内存对齐填充
联合体：总大小 = 最大成员的字节大小，所有成员共用一块空间

 


题目：C 语言中 指针函数 和 函数指针 有什么区别？
1. 指针函数
本质：是一个函数
特征：返回值是指针
格式：int* fun();
意思：这个函数执行完，返回一个地址（指针）
2. 函数指针
本质：是一个指针变量
特征：指向一个函数，存的是函数的地址
格式：int (*fun)();
意思：用一个指针，指向某一个函数，可以用来调用函数
// 指针函数：返回 int* 指针
int* test(int a, int b)
{
    static int num = 10;
    return &num;  
}


